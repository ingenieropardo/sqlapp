SELECT NUMERO_IDENTIFICACION, NOMBRE_IMPORTADOR, I.FECHA_CREACION, ED.TIPO_REGIMEN, COUNT(ED.NUMERO_DO) OPERACIONES
FROM IMPORTADORES I
LEFT OUTER JOIN ESTADOS_DO ED ON ED.ID_IMPORTADOR=I.ID_IMPORTADOR
WHERE YEAR(I.FECHA_CREACION)=2018
GROUP BY I.ID_IMPORTADOR, ED.TIPO_REGIMEN
